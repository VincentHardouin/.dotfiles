[user]
	email = vincent.hardouin@pix.fr
	name = Vincent Hardouin
[core]
	editor = vim
[alias]
	# Pop your last commit out of the history! No change lost, just unindexed
    	pop = reset HEAD^
	# Add a file/directory to your .gitignore
    	ignore = "!f() { echo \"$1\" >> .gitignore; }; f"
	clean = "git fetch --prun && git branch --merged origin/master|xargs git branch -d"
[pull]
	rebase = true
[rerere]
	enabled = true
[commit]
	verbose = true
